import React from 'react';
import { Switch, Route } from 'react-router-dom'

const Types = () => {
  return (<div className="Types">
            This is where the types will go
          </div>);
};

export default Types;
